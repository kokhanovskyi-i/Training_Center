<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_training_center_teacher_category_list" model="ir.ui.view">
        <field name="name">training.center.teacher.category.list</field>
        <field name="model">training.center.teacher.category</field>
        <field name="arch" type="xml">
            <list>
                <field name="sequence" widget="handle"/>
                <field name="name"/>
            </list>
        </field>
    </record>

    <record id="view_training_center_teacher_category_form" model="ir.ui.view">
        <field name="name">training.center.teacher.category.form</field>
        <field name="model">training.center.teacher.category</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="sequence"/>
                    </group>

                    <notebook>
                        <page string="Teachers">
                            <field name="teacher_ids" readonly="1">
                                <list>
                                    <field name="name"/>
                                    <field name="specialty"/>
                                    <field name="email"/>
                                    <field name="phone"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_training_center_teacher_category" model="ir.actions.act_window">
        <field name="name">Teacher Categories</field>
        <field name="res_model">training.center.teacher.category</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>